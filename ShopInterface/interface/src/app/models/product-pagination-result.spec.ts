import { ProductPaginationResult } from './product-pagination-result';

describe('ProductPaginationResult', () => {
  it('should create an instance', () => {
    expect(new ProductPaginationResult()).toBeTruthy();
  });
});
